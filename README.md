# AdBlock DoH Server

DNS-over-HTTPS ad blocker running on Render's free tier.

## Features
- Blocks ads at DNS level
- DNS-over-HTTPS (encrypted)
- Web dashboard with stats
- Auto-updating blocklists
- 100% free hosting

## Deployment
Automatically deploys to Render on push to main branch.